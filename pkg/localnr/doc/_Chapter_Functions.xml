<?xml version="1.0" encoding="UTF-8"?>

<!-- This is an automatically generated file. -->
<Chapter Label="Chapter_Functions">
<Heading>Functions</Heading>

<Section Label="Chapter_Functions_Section_Group_functions">
<Heading>Group functions</Heading>

<ManSection>
  <Prop Arg="G" Name="IsMinimalNonAbelianGroup" />
 <Returns>a boolean
</Returns>
 <Description>
 The argument is a group <Math>G</Math>.
 The output is <C>true</C> if <Math>G</Math> is a minimal non-abelian group,
 otherwise the output is <C>false</C>.
 </Description>
</ManSection>

 Recall that each finite non-abelian group whose proper subgroups are
 abelian is called a <Emph>Miller-Moreno group</Emph> or, in other terminology,
 a <Emph>minimal non-abelian group</Emph>.
<Example><![CDATA[
gap> H:=SmallGroup(120,4);       
<pc group of size 120 with 5 generators>
gap> IsMinimalNonAbelianGroup(H);
false
gap> K:=SmallGroup(16,6);
<pc group of size 16 with 4 generators>
gap> IsMinimalNonAbelianGroup(K);
true
gap> IsMinimalNonAbelianGroup(SmallGroup(16,8));
false
]]></Example>

<ManSection>
  <Prop Arg="G" Name="IsMetacyclicPGroup" />
 <Returns>a boolean
</Returns>
 <Description>
 The argument is a group <Math>G</Math>.
 The output is <C>true</C> if <Math>G</Math> is a metacyclic <Math>p</Math>-group,
 otherwise the output is <C>false</C>.
 </Description>
</ManSection>

<Example><![CDATA[
gap> IsMetacyclicPGroup(K);
true
gap> IsMetacyclicPGroup(SmallGroup(81,4));
true
gap> IsMetacyclicPGroup(SmallGroup(81,15));
false
]]></Example>

<ManSection>
  <Oper Arg="G" Name="EndoOrbitsOfGroup" />
 <Returns>EndoOrbitsOfGroup
</Returns>
 <Description>
 The argument is a group <Math>G</Math>.
 The output is a list of pairs <C>[x,H]</C>, where <C>x</C> is a
 representative of an endo-orbit of <Math>G</Math> and <C>H</C> is the set of all
 images of <C>x</C> under endomorphisms of <Math>G</Math>.
 </Description>
</ManSection>

<Example><![CDATA[
gap> D:=SmallGroup(81,2); 
<pc group of size 81 with 4 generators>
gap> T:=EndoOrbitsOfGroup(D);;
gap> Length(T);
1
gap> Size(T[1][2]);
81
]]></Example>

<ManSection>
  <Prop Arg="G" Name="IsEndoCyclicGroup" />
 <Returns>a boolean
</Returns>
 <Description>
 The argument is a group <Math>G</Math>.
 The output is <C>true</C> if <Math>G</Math> is an endocyclic group,
 otherwise the output is <C>false</C>.
 </Description>
</ManSection>

 Let <Math>G</Math> be a group and <Math>End G</Math> be the set of all its endomorphisms,
 which can be considered as a semigroup with respect to composition.
 For each <Math>g\in G</Math> we denote by <Math>g^{End G}</Math> the set <Math>\{g^\alpha| \alpha\in End G\}</Math>
of all images of the element <Math>g</Math> with respect to endomorphisms of <Math>End G</Math>.
 A group <Math>G</Math> is called <Emph>endocyclic</Emph> if it contains an element <Math>g</Math> with <Math>G=g^{End G}</Math>.
<Example><![CDATA[
gap> IsEndoCyclicGroup(D);
true
]]></Example>

</Section>

<Section Label="Chapter_Functions_Section_Nearring_functions">
<Heading>Nearring functions</Heading>

<ManSection>
  <Attr Arg="R" Name="UnitsOfNearRing" />
 <Returns>a list
</Returns>
 <Description>
 The argument is a nearring <Math>R</Math>.
 The output is a list of units if <Math>R</Math> is a nearring with identity,
 otherwise the output is an empty list.
 </Description>
</ManSection>

<Example><![CDATA[
gap> N:=LocalNearRing(32,5,16,3,8);
ExplicitMultiplicationNearRing ( <pc group of size 32 with 
5 generators> , multiplication )
gap> U:=UnitsOfNearRing(N);
[ (f1), (f1*f5), (f1*f4), (f1*f4*f5), (f1*f3), (f1*f3*f5), (f1*f3*f4),  
 (f1*f3*f4*f5), (f1*f2), (f1*f2*f5), (f1*f2*f4), (f1*f2*f4*f5), (f1*f2*f3), 
 (f1*f2*f3*f5), (f1*f2*f3*f4), (f1*f2*f3*f4*f5) ]
gap> Un:=NearRingUnits(N);;
gap> U=Un;
true
gap> L:=LibraryNearRing(SmallGroup(6,1),3);
#I  using isomorphic copy of the group
LibraryNearRing(6/2, 3)
gap> UnitsOfNearRing(L);
[  ]
]]></Example>

<ManSection>
  <Prop Arg="R" Name="IsLocalNearRing" />
 <Returns>a boolean
</Returns>
 <Description>
 The argument is a nearring <Math>R</Math>.
 The output is <C>true</C> if <Math>R</Math> is a local nearring,
 otherwise the output is <C>false</C>.
 </Description>
</ManSection>

<Example><![CDATA[
gap> H:=SmallGroup(16,6);
<pc group of size 16 with 4 generators>
gap> A:= AutomorphismNearRing(H);
AutomorphismNearRing( <pc group of size 16 with 4 generators> )
gap>  Size(A);
64
gap> IsLocalNearRing(A);
true
gap> K:=LibraryNearRingWithOne(SmallGroup(8,2),1);  
#I  using isomorphic copy of the group
LibraryNearRing(8/2, 814)
gap> IsLocalNearRing(K);
false
]]></Example>

<ManSection>
  <Prop Arg="R" Name="IsLocalRing" />
 <Returns>a boolean
</Returns>
 <Description>
 The argument is a local nearring <Math>R</Math>.
 The output is <C>true</C> if <Math>R</Math> is a local ring,
 otherwise the output is <C>false</C>.
 </Description>
</ManSection>

<Example><![CDATA[
gap> L:=AllLocalNearRings(16,14,8,4);;
gap> Size(L);
24
gap> F:=Filtered(L,x->IsLocalRing(x));;
gap> Size(F);
1
]]></Example>

<ManSection>
  <Attr Arg="R" Name="NearRingNonUnits" />
 <Returns>a set
</Returns>
 <Description>
 The argument is a nearring <Math>R</Math>.
 The output is the set of non-invertible elements of <Math>R</Math>. 
 </Description>
</ManSection>

<Example><![CDATA[
gap> T:=LocalNearRing(49,2,42,1,1); 
ExplicitMultiplicationNearRing ( <pc group of size 49 with 
2 generators> , multiplication )
gap> Nu:=NearRingNonUnits(T);
[ (<identity> of ...), (f2), (f2^2), (f2^3), (f2^4), (f2^5), (f2^6) ]
gap> Size(Nu);
7
gap> R:=LibraryNearRing(SmallGroup(8,4),3);
#I  using isomorphic copy of the group
LibraryNearRing(8/5, 3)
gap> N:=SortedList(NearRingNonUnits(R)); 
[ (()), ((1,2,3,4)(5,6,7,8)), ((1,3)(2,4)(5,7)(6,8)), ((1,4,3,2)(5,8,7,6)), 
  ((1,5,3,7)(2,8,4,6)), ((1,6,3,8)(2,5,4,7)), ((1,7,3,5)(2,6,4,8)), 
  ((1,8,3,6)(2,7,4,5)) ]
]]></Example>

<ManSection>
  <Oper Arg="R,gens" Name="SubNearRingByGenerators" />
 <Returns>a subnearring
</Returns>
 <Description>
 The arguments are a nearring <Math>R</Math> and generators <Math>gens</Math> of <Math>R</Math>.
 The output is the subnearring generated by <Math>gens</Math>.
 </Description>
</ManSection>

<Example><![CDATA[
gap> B:=LocalNearRing(25,2,20,3,1); 
ExplicitMultiplicationNearRing ( <pc group of size 25 with 2 generators> , multiplication )
gap> D:=DistributiveElements(B);;
gap> Size(D);
5
gap> Rs:=SubNearRingByGenerators(B,D);;
gap> Size(Rs);
5
gap> IsDgNearRing(B);
false
gap> IsDgNearRing(Rs);
true
]]></Example>

<ManSection>
  <Attr Arg="R" Name="NonUnitsAsAdditiveSubgroup" />
 <Returns>a subgroup
</Returns>
 <Description>
 The argument is a local nearring <Math>R</Math>.
 The output is the additive subgroup of non-units of <Math>R</Math>.
 </Description>
</ManSection>

<Example><![CDATA[
gap> T:=LocalNearRing(125,4,100,9,1); 
ExplicitMultiplicationNearRing ( <pc group of size 125 with 
3 generators> , multiplication )
gap> L:=NonUnitsAsAdditiveSubgroup(T);
Group([ <identity> of ..., f2, f3, f2^2, f2*f3, f3^2, f2^3, f2^2*f3, f2*f3^2, f3^3, f2^4, 
  f2^3*f3, f2^2*f3^2, f2*f3^3, f3^4, f2^4*f3, f2^3*f3^2, f2^2*f3^3, f2*f3^4, f2^4*f3^2, 
  f2^3*f3^3, f2^2*f3^4, f2^4*f3^3, f2^3*f3^4, f2^4*f3^4 ])
gap> IdGroup(L);
[ 25, 2 ]
]]></Example>

<ManSection>
  <Attr Arg="R" Name="NonUnitsAsNearRingIdeal" />
 <Returns>an ideal
</Returns>
 <Description>
 The argument is a local nearring <Math>R</Math>.
 The output is the ideal generated by all non-invertible elements of <Math>R</Math>.
 </Description>
</ManSection>

<Example><![CDATA[
gap> I:=NonUnitsAsNearRingIdeal(T);
< nearring ideal >
gap> Size(I);
25
]]></Example>

<ManSection>
  <Attr Arg="R" Name="MultiplicativeSemigroupOfNearRing" />
 <Returns>a semigroup
</Returns>
 <Description>
 The argument is a nearring <Math>R</Math>.
 The output is the multiplicative semigroup of <Math>R</Math>.
 </Description>
</ManSection>

<Example><![CDATA[
gap> B:=LocalNearRing(16,10,8,2,7);;
gap> M:=MultiplicativeSemigroupOfNearRing(B);
<semigroup of size 16, with 7 generators>
gap> Size(M);
16
]]></Example>

<ManSection>
  <Attr Arg="R" Name="NonUnitsAsMultiplicativeSemigroup" />
 <Returns>a semigroup
</Returns>
 <Description>
 The argument is a nearring <Math>R</Math>.
 The output is the multiplicative semigroup of non-units of <Math>R</Math>.
 </Description>
</ManSection>

<Example><![CDATA[
gap> Nm:=NonUnitsAsMultiplicativeSemigroup(B);
<semigroup with 8 generators>
gap> Size(Nm);
8
]]></Example>

<ManSection>
  <Prop Arg="R" Name="IsOneGeneratedNearRing" />
 <Returns>a boolean
</Returns>
 <Description>
 The argument is a nearring <Math>R</Math>.
 The output is <C>true</C> if <Math>R</Math> is a nearring generated by one element,
 otherwise the output is <C>false</C>.
 </Description>
</ManSection>

<Example><![CDATA[
gap> D:=LocalNearRing(49,2,42,4,1);
ExplicitMultiplicationNearRing ( <pc group of size 49 with 2 generators> , multiplication )
gap> IsOneGeneratedNearRing(D);
true
gap> H:=LocalNearRing(16,14,8,2,3);   
ExplicitMultiplicationNearRing ( <pc group of size 16 with 
4 generators> , multiplication )
gap> IsOneGeneratedNearRing(H);    
false
]]></Example>

<ManSection>
  <Oper Arg="R,Un" Name="AutomorphismsAssociatedWithNearRingUnits" />
 <Returns>a list of automorphisms
</Returns>
 <Description>
 The arguments are a nearring <Math>R</Math> with identity and a set of units <Math>Un</Math> of <Math>R</Math>.
 The output is the list of automorphisms of the additive group of <Math>R</Math>
 associated with the units in <Math>Un</Math>.
 </Description>
</ManSection>

 A subgroup <Math>A</Math> of the automorphism group <Math>Aut R^+</Math> of the additive group of 
 the nearring <Math>R</Math> with identity isomorphic to the multiplicative group <Math>R^*</Math>
 and satisfying the condition <Display>i^A=\{i^a\mid a\in A\}=R^*</Display> is called
 the subgroup of <Math>Aut R^+</Math> associated with <Math>R^*</Math>.
<Example><![CDATA[
gap> S:=UnitsOfNearRing(D);
[ (f1), (f1*f2), (f1*f2^2), (f1*f2^3), (f1*f2^4), (f1*f2^5), (f1*f2^6), (f1^2), (f1^2*f2), 
  (f1^2*f2^2), (f1^2*f2^3), (f1^2*f2^4), (f1^2*f2^5), (f1^2*f2^6), (f1^3), (f1^3*f2), 
  (f1^3*f2^2), (f1^3*f2^3), (f1^3*f2^4), (f1^3*f2^5), (f1^3*f2^6), (f1^4), (f1^4*f2), 
  (f1^4*f2^2), (f1^4*f2^3), (f1^4*f2^4), (f1^4*f2^5), (f1^4*f2^6), (f1^5), (f1^5*f2), 
  (f1^5*f2^2), (f1^5*f2^3), (f1^5*f2^4), (f1^5*f2^5), (f1^5*f2^6), (f1^6), (f1^6*f2), 
  (f1^6*f2^2), (f1^6*f2^3), (f1^6*f2^4), (f1^6*f2^5), (f1^6*f2^6) ]
gap> A:=AutomorphismsAssociatedWithNearRingUnits(D,S);;
gap> Size(A);
42
]]></Example>

<ManSection>
  <Oper Arg="R, Elm" Name="EndomorphismsAssociatedWithNearRingElements" />
 <Returns>a list of endomorphisms
</Returns>
 <Description>
 The arguments are a nearring <Math>R</Math> and a set <Math>Elm</Math> of nearring elements.
 The output is the endomorphisms associated with nearring elements.
 </Description>
</ManSection>

<Example><![CDATA[
gap> Nu:=NearRingNonUnits(D);
[ (<identity> of ...), (f2), (f2^2), (f2^3), (f2^4), (f2^5), (f2^6) ]
gap> En:=EndomorphismsAssociatedWithNearRingElements(D,Nu);;
gap> Size(En);
7
]]></Example>

<ManSection>
  <Oper Arg="R" Name="SemidirectProductAssociatedWithNearRing" />
 <Returns>a semidirect product
</Returns>
 <Description>
 The argument is a nearring <Math>R</Math> with identity.
 The output is the semidirect product associated with the nearring <Math>R</Math>.
 </Description>
</ManSection>

<Example><![CDATA[
gap> T:=LocalNearRing(25,2,20,2,1);             
ExplicitMultiplicationNearRing ( <pc group of size 25 with 2 generators> , multiplication )
gap> SemidirectProductAssociatedWithNearRing(T);
<pc group with 5 generators>
gap> Size(last);
500
]]></Example>

<ManSection>
  <Oper Arg="R,H" Name="IsCircleSubgroupOfNearRing" />
 <Returns>a boolean
</Returns>
 <Description>
 The arguments are a nearring <Math>R</Math> with identity and 
 a subgroup <Math>H</Math> of the additive group of <Math>R</Math>.
 The output is <C>true</C> if <Math>H</Math> is a circle subgroup of the nearring <Math>R</Math>,
 otherwise the output is <C>false</C>.
 </Description>
</ManSection>

<Example><![CDATA[
gap> Sg:=Subgroups(GroupReduct(T));;
gap> Size(Sg);
8
gap> F:=Filtered(Sg,x->IsCircleSubgroupOfNearRing(T,x));
[ Group([  ]), Group([ f2 ]) ]
]]></Example>

<ManSection>
  <Oper Arg="R,H" Name="FactorizedGroupAssociatedWithCircleSubgroupOfNearRing" />
 <Returns>a group
</Returns>
 <Description>
 The arguments are a nearring <Math>R</Math> with identity and 
 a circle subgroup <Math>H</Math> of <Math>R</Math>.
 The output is the semidirect product associated with <Math>H</Math> and the
 automorphisms determined by the corresponding units.
 </Description>
</ManSection>

<Example><![CDATA[
gap> FG:=FactorizedGroupAssociatedWithCircleSubgroupOfNearRing(T,F[2]);
<pc group with 2 generators>
gap> IdGroup(FG);
[ 25, 2 ]
]]></Example>

<ManSection>
  <Attr Arg="R" Name="ConstantPartOfNearRing" />
 <Returns>a constant part
</Returns>
 <Description>
 The argument is a nearring <Math>R</Math>.
 The output is the constant part of the nearring <Math>R</Math>.
 </Description>
</ManSection>

<Example><![CDATA[
gap> H:=LocalNearRing(361,2,342,7,7);
ExplicitMultiplicationNearRing ( <pc group of size 361 with 
2 generators> , multiplication )
gap> C:=ConstantPartOfNearRing(H);;
gap> Size(C);
19
]]></Example>

<ManSection>
  <Attr Arg="R" Name="ZeroSymmetricPartOfNearRing" />
 <Returns>a zero-symmetric part
</Returns>
 <Description>
 The argument is a nearring <Math>R</Math>.
 The output is the zero-symmetric part of the nearring <Math>R</Math>.
 </Description>
</ManSection>

<Example><![CDATA[
gap> ZeroSymmetricPartOfNearRing(H);;
gap> Size(last);
19
]]></Example>

<ManSection>
  <Attr Arg="R" Name="GroupOfUnitsAsGroupOfAutomorphisms" />
 <Returns>a group
</Returns>
 <Description>
 The argument is a nearring <Math>R</Math>.
 The output is a group of automorphisms of the additive group of <Math>R</Math>
 that is isomorphic to the group of units of <Math>R</Math>.
 </Description>
</ManSection>

<Example><![CDATA[
gap> M:=LocalNearRing(27,4,18,3,2);  
ExplicitMultiplicationNearRing ( <pc group of size 27 with 3 generators> , multiplication )
gap> GroupOfUnitsAsGroupOfAutomorphisms(M);
<group of size 18 with 2 generators>
gap> Size(last);
18                
]]></Example>

<ManSection>
  <Oper Arg="R, r" Name="IsDistributiveElementOfNearRing" />
 <Returns>a boolean
</Returns>
 <Description>
 The argument is a nearring <Math>R</Math> and an element <Math>r</Math>.
 The output is <C>true</C> if <Math>r</Math> is a distributive element of the nearring <Math>R</Math>,
 otherwise the output is <C>false</C>.
 </Description>
</ManSection>

<Example><![CDATA[
gap> D:=LocalNearRing(49,2,42,6,1); 
ExplicitMultiplicationNearRing ( <pc group of size 49 with 
2 generators> , multiplication )
gap> h:=List(D);;
gap> d:=h[3];
(f2^2)
gap> IsDistributiveElementOfNearRing(D,d);
true
]]></Example>

<ManSection>
  <Prop Arg="R" Name="IsSemiDistributiveNearRing" />
 <Returns>a boolean
</Returns>
 <Description>
 The argument is a nearring <Math>R</Math>.
 The output is <C>true</C> if <Math>R</Math> is a semidistributive nearring,
 otherwise the output is <C>false</C>.
 </Description>
</ManSection>

<Example><![CDATA[
gap> N:=LocalNearRing(16,10,8,2,7); 
ExplicitMultiplicationNearRing ( <pc group of size 16 with 4 generators> , multiplication )
gap> IsSemiDistributiveNearRing(N);
true
]]></Example>

<ManSection>
  <Prop Arg="R" Name="IsNearRingWithIdentity" />
 <Returns>a boolean
</Returns>
 <Description>
 The argument is a nearring <Math>R</Math>.
 The output is <C>true</C> if <Math>R</Math> is a nearring with identity,
 otherwise the output is <C>false</C>.
 </Description>
</ManSection>

<Example><![CDATA[
gap> N:=LocalNearRing(343,5,294,8,2);    
ExplicitMultiplicationNearRing ( <pc group of size 343 with 
3 generators> , multiplication )
gap> IsNearRingWithOne(N);
false
gap> Identity(N);
(f1)
gap> IsNearRingWithIdentity(N);
true
]]></Example>

<ManSection>
  <Oper Arg="R,H" Name="IsSubNearRing" />
 <Returns>a boolean
</Returns>
 <Description>
 The arguments are a nearring <Math>R</Math> with identity and 
 a subgroup <Math>H</Math> of the additive group of <Math>R</Math>.
 The output is <C>true</C> if <Math>H</Math> is the additive group of a subnearring of <Math>R</Math>,
 otherwise the output is <C>false</C>.
 </Description>
</ManSection>

<Example><![CDATA[
gap> T:=LocalNearRing(49,2,42,1,2);        
ExplicitMultiplicationNearRing ( <pc group of size 49 with 
2 generators> , multiplication )
gap> G:=GroupReduct(T);                    
<pc group of size 49 with 2 generators>
gap> S:=Subgroups(G);;
gap> Size(S);
10
gap> IsSubNearRing(T,S[3]);
true
gap> IsSubNearRing(T,S[9]); 
false
gap> D:=SmallGroup(7,1);;
gap> IsSubNearRing(T,D);   
false
]]></Example>

</Section>

</Chapter>

